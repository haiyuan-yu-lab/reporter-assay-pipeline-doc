# `yulab_reporter_export`

ExogeneousSequences export from Step 9 activity output plus references.

```bash
yulab_reporter_export --help
```

> Scaffold page — flag-level detail will be added in a later docs update.
